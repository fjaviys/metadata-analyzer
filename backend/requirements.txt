# Metadata Analyzer — backend
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
reportlab==4.2.5
python-multipart==0.0.20
websockets==14.1
# exiftool se instala a nivel de SO (ver Dockerfile), no por pip.
# psycopg[binary]==3.2.3   # opcional, solo si se migra a PostgreSQL
